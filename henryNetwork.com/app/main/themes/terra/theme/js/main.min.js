function copy(copyValue) {
    navigator.clipboard.writeText(copyValue);
    Swal.fire({
        title: null,
        text: "Başarıyla kopyalandı!",
        icon: 'success',
        confirmButtonText: "Tamam"
    });
}

$(document).ready(function () {
    $('.select').select2({
        minimumResultsForSearch: -1
    });
});

$(document).ready(function () {
    $(".hide-password").css('display', 'none');
    $(".show-password, .hide-password").on('click', function () {
        var passwordId = $(this).parents('.password-area').find('input').attr('id');
        if ($(this).hasClass('show-password')) {
            $("#" + passwordId).attr("type", "text");
            $(this).parent('.password-area').find(".show-password").css('display', 'none');
            $(this).parent('.password-area').find(".hide-password").css('display', 'block');
        } else {
            $("#" + passwordId).attr("type", "password");
            $(this).parent('.password-area').find(".show-password").css('display', 'block');
            $(this).parent('.password-area').find(".hide-password").css('display', 'none');
        }
    });
});

$('img[data-enlargable]').addClass('img-enlargable').click(function () {
    var src = $(this).attr('src');
    $('<div>').css({
        background: 'RGBA(0,0,0,.8) url(' + src + ') no-repeat center',
        backgroundSize: 'contain',
        width: '100%', height: '100%',
        position: 'fixed',
        zIndex: '10000',
        top: '0', left: '0',
        cursor: 'zoom-out'
    }).click(function () {
        $(this).remove();
    }).appendTo('body');
});
var swiper = new Swiper(".mySwiper", {
      spaceBetween: 10,
      slidesPerView: 4,
      freeMode: true,
      watchSlidesProgress: true,
    });
    var swiper2 = new Swiper(".mySwiper2", {
      spaceBetween: 10,
      navigation: {
        nextEl: ".swiper-button-next",
        prevEl: ".swiper-button-prev",
      },
      thumbs: {
        swiper: swiper,
      },
    });